name: Pipeline CI/CD (AWS)

on:
  push:
    branches: [ "main" ]

env:
    AS_API_NAME: as-sensors-api
    AS_API_DOCKERFILE: ./as-sensors-api/Dockerfile

jobs:
  test:
    name: Running Tests
    runs-on: ubuntu-latest
    steps:
    
      - name: Download source
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Dependency Restore
        run: dotnet restore

      - name: Build with dotnet
        run: dotnet build --configuration Release --no-restore

      - name: Domain Tests
        run: dotnet test ./as-sensors-domain-tests/as-sensors-domain-tests.csproj --configuration Release --no-build --verbosity normal

  deploy:
    name: Deploy to Production
    needs: test
    if: success()
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    # Login na AWS
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
        aws-region: ${{ vars.AWS_REGION }}

    # Login no ECR
    - name: Login to Amazon ECR
      uses: aws-actions/amazon-ecr-login@v2

    # Build & Push da API
    - name: Build and push API image
      run: |
        docker build -t ${{env.AS_API_NAME}} -f ${{env.AS_API_DOCKERFILE}} .
        docker tag ${{env.AS_API_NAME}}:latest ${{ vars.AWS_ACCOUNT_ID }}.dkr.ecr.${{ vars.AWS_REGION }}.amazonaws.com/${{env.AS_API_NAME}}:latest
        docker push ${{ vars.AWS_ACCOUNT_ID }}.dkr.ecr.${{ vars.AWS_REGION }}.amazonaws.com/${{env.AS_API_NAME}}:latest

    # Configura kubectl para EKS
    - name: Update kubeconfig
      run: |
        aws eks update-kubeconfig \
          --name ${{ vars.EKS_CLUSTER_NAME }} \
          --region ${{ vars.AWS_REGION }}
    
    - name: Render Kubernetes manifest
      env:
        AWS_ACCOUNT_ID: ${{ vars.AWS_ACCOUNT_ID }}
        AWS_REGION: ${{ vars.AWS_REGION }}
        IMAGE_TAG: ${{ github.sha }}
      run: |
        mkdir -p rendered
        envsubst < k8s/sensors-api-deployment.yaml > rendered/sensors-api-deployment.yaml
        cp k8s/sensors-api-config.yaml rendered/sensors-api-config.yaml
        cp k8s/sensors-api-service.yaml rendered/sensors-api-service.yaml

    # Deploy no Kubernetes
    - name: Apply Kubernetes manifests
      run: |
        kubectl apply -f rendered/
