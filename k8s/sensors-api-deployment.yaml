apiVersion: apps/v1
kind: Deployment
metadata:
  name: as-sensors-api
  labels:
    app: as-sensors-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: as-sensors-api
  template:
    metadata:
      labels:
        app: as-sensors-api
    spec:
      containers:
        - name: as-sensors-api
          image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/as-sensors-api:latest
          ports:
            - containerPort: 8080
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: AWS_REGION
              value: "${AWS_REGION}"
            - name: AWS_DEFAULT_REGION
              value: "${AWS_REGION}"
          envFrom:
            - secretRef:
                name: aws-credentials
            - configMapRef:
                name: as-sensors-config
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
