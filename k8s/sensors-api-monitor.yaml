apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: as-sensors-api
  namespace: default
  labels:
    release: stable
spec:
  namespaceSelector:
    matchNames:
       - default
  selector:
    matchLabels:
      app: as-sensors-api
  endpoints:
  - port: http
    path: /metrics
    interval: 10s