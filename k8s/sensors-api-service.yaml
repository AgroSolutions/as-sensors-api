apiVersion: v1
kind: Service
metadata:
  name: as-sensors-api-service
spec:
  selector:
    app: as-sensors-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
