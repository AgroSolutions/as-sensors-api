apiVersion: v1
kind: Service
metadata:
  name: as-sensors-api-service
  labels:
    app: as-sensors-api
spec:
  selector:
    app: as-sensors-api
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  type: LoadBalancer
